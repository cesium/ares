---
import { Picture } from "astro:assets";
import background from "~/assets/texture-lab.jpg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Picture
  class="h-full w-full object-cover"
  src={background}
  widths={widths}
  sizes={sizes}
  alt=""
/>
